name: trouble_pad
version: 1.0.0+1
publish_to: 'none'
description: A Flutter Web app for recording troubleshooting history.

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
